import zlib # NOQA
import base64 # NOQA
import tempfile # NOQA

# Decompress resourcess
COMMAND = "{command}"
FILESYSTEM = eval(zlib.decompress(base64.b64decode("{filesystem}")))

if __name__ == "__main__":
	# Create temporary path for filesystem


	try:
		pass
	except:
		pass